# SovereignForge backend dependencies
# No pinned versions — let pip resolve compatible wheels for your Python version

fastapi
uvicorn[standard]
websockets
pydantic>=2.0

# HTTP client (calls Ollama)
httpx

# Document processing
pytesseract
Pillow
pdf2image
pymupdf
python-docx

# New output tools
python-pptx
openpyxl

# Local RAG / Knowledge Base (fully offline after first model download)
sentence-transformers
numpy

# PDF fixture generation (for demo files)
fpdf2

# File utilities (python-multipart is required by FastAPI for UploadFile endpoints)
aiofiles
python-multipart

# Testing
pytest
pytest-asyncio

# Docker SDK (for sandbox)
docker

# mitmproxy is not needed here: docker-compose runs it as its own container.
# For local dev (scripts/start_dev.ps1) install requirements-dev.txt instead.
